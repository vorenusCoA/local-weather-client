export class Main {

    temp: number
    temp_min: number
    temp_max: number

    
    constructor(temp: number, temp_min: number, temp_max: number) {
        this.temp = temp,
        this.temp_min = temp_min,
        this.temp_max = temp_max
    }
 
}